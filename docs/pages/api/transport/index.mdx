# Transport

The transport layer is the part that handles the actual peer-to-peer communication and EIP-1193 request/response messaging.

## Supported Protocols

Currently openlv relies on [WebRTC](/api/transport/webrtc) for the transport layer.

