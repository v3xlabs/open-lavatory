# Gun

[Gun](https://gun.eco/docs) is a decentralized database that is used to store and retrieve data.

```typescript
import { gundb } from '@openlv/signaling/gundb';

const session = await createSession({ p: 'gundb', s: 'wss://try.axe.eco/gun' }, gundb);
```

## URL Format

```yaml
wss://try.axe.eco/gun
```

## Hosted Infrastructure (try.axe.eco)

## Self-Hosted

You can run your own GunDB instance using [documentation]().
