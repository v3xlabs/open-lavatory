# Configuration

```ts [wagmi.ts]
import { http, createConfig, createStorage } from 'wagmi'
import { mainnet } from 'wagmi/chains'
import { openlv } from '@openlv/connector' // [!code ++]

export const wagmiConfig = createConfig({
  chains: [mainnet],
  connectors: [
    openlv({
      signaling: [
        { // [!code ++]
            type: 'mqtt', // [!code ++]
            url: 'wss://test.mosquitto.org:8081/mqtt', // [!code ++]
        } // [!code ++]
      ],
    })
  ],
  transports: {
    [mainnet.id]: http(),
  },
})
```
